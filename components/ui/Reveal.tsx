/**
 * Backward-compatible re-export.
 * The original Reveal component is superseded by BlurReveal which uses
 * a blur-to-clear entrance instead of a simple Y-slide, matching the
 * luxury typography-centered animation system.
 */
export { BlurReveal as Reveal } from './BlurReveal';